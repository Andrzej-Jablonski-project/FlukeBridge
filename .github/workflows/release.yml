name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create GitHub Release (auto-publish)
        uses: softprops/action-gh-release@v1
        with:
          draft: false
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }} – Fluke Bridge
          body_path: CHANGELOG.md
          files: |
            FlukeBridge-ver${{ replace(github.ref_name, 'v', '') }}-XIAO-ESP32C3.bin
            SHA256SUMS.txt
